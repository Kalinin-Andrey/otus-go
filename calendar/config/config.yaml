server:
  httplisten:  "localhost:8080"

log:
  encoding:     "json"
  outputpaths:
    - "log/app.log"
  level:        "debug"

db:
  dsn:          ""

repository:
  type:       "inmemory"